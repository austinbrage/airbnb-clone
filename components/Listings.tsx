import { View, Text } from "react-native"

interface Props {
    listings: unknown[]
    category: string
}

export default function Listings({ listings, category }: Props) {
    return (
        <View>
            <Text></Text>
        </View>
    )
}